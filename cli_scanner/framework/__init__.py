"""Universal trading-bot framework: reusable core for many strategies.

Subpackages:
- ``core``       — trading calendar, universes, strategy config loading
- ``risk``       — sizing, risk manager, kill switch, equity tracking
- ``execution``  — order lifecycle manager, broker reconciliation, lifecycle
- ``positions``  — position guards (assignment risk) and, later, exit rules
- ``data``       — point-in-time data catalog, model registry
"""
